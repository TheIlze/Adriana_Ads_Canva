# Frontend Dockerfile
FROM node:20-alpine

WORKDIR /app

# Copy all files
COPY . .

# Install dependencies
RUN npm install

# Build the React app for production (if needed)
RUN npm run build

# Expose port and run dev server (or use static server)
EXPOSE 8080

CMD ["npm", "run", "start"]
